import { Stack } from '@mui/material';
import React from 'react';
import EncryptKeysForm from './EncryptKeysForm';

const BasicSettings = () => {
  return (
    <Stack>
      <EncryptKeysForm />
    </Stack>
  );
};

export default BasicSettings;
